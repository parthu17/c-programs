#include <stdio.h>
#include <stdlib.h>
void stringcat (char*,char*,int,int);
int main(){
 int n,m,i;
 char *q,*p;
 printf("enter the number of characters of string 1");
 scanf("%d",&n);
 p=(char*)malloc(n*sizeof(char));
 printf("enter the number of characters of string 2");
 scanf("%d",&m);
 q=(char*)malloc(m*sizeof(char));
 printf("enter the string 1");
 for(i=0;i<n-1;i++){
  scanf("%c",p+i);
  }
  printf("enter the string 2");
 for(i=0;i<m-1;i++){
  scanf("%c",q+i);
  }
  printf("the concatenated string is ");
  for(i=0;*(p+i)!='\0';i++){
   printf("%c",*(p+i));
  return 0;
}
int stringcat(char*p,char *q, int n,int m){
 int i;
 p=realloc(p,(m+n)* sizeof(char));
 for(i=m;i<n;i++){
  *(p+i) = *(q+
 
}
