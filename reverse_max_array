#include <stdio.h>
#include <stdlib.h>
int reverse(int*,int);
int max(int*,int);
itn main(){
int n,i,*p,even=0,odd=0;
printf("enter length of array");
scanf("%d",&n);
p= int* calloc(n,sizeof(int));
pri
